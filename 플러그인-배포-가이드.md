# Claude 스킬을 플러그인으로 묶어 마켓에 등록·배포하기

이 문서는 저장소의 스킬들을 **플러그인 1개**로 묶어 **마켓플레이스에 등록**하고,
**Claude Code(CLI)** 와 **Claude Cowork(데스크톱 앱)** 양쪽에서 설치할 수 있게 만든 전 과정을 정리한 것입니다.
실제로 실행·검증한 명령과 중간에 막혔던 지점까지 그대로 담았습니다.

**이 저장소의 실제 결과물**

| 항목 | 값 |
|------|-----|
| 마켓플레이스 저장소 | `stiron92-byte/claude-skills` (public) |
| 마켓플레이스 이름 | `claude-skills` |
| 플러그인 이름 | `claude-skills-book` |
| 포함 스킬 (8개) | doc-automation · excel-automation · meeting-minutes · content-research · data-collector · content-repurpose · generate-shorts · narration-video |

---

## 0. 개념: 스킬 → 플러그인 → 마켓플레이스

```
마켓플레이스 (GitHub 저장소 + .claude-plugin/marketplace.json)
└── 플러그인 (사용자가 설치하는 단위)
    ├── skills/        ← 스킬 여러 개
    ├── commands/      (선택)
    ├── agents/        (선택)
    ├── hooks/         (선택)
    └── .mcp.json      (선택, MCP 서버)
```

| 용어 | 정의 |
|------|------|
| **스킬(Skill)** | `SKILL.md` 1개 + 부속 파일(scripts/templates/references). Claude가 상황에 맞게 자동 호출 |
| **플러그인(Plugin)** | 스킬·커맨드·에이전트·MCP를 묶은 **설치 단위**. 보통 스킬 여러 개를 포함 |
| **마켓플레이스(Marketplace)** | 플러그인 목록을 담은 **공개 git 저장소**. `.claude-plugin/marketplace.json`이 핵심 |

> 핵심: **플러그인 1개 = 스킬 여러 개.** 사용자는 플러그인 하나만 설치하면 안의 스킬이 전부 등록됩니다.

---

## 1. 배포용 스킬 구조 만들기

### ① 규칙 — 폴더명 = 스킬 `name`
플러그인 스킬은 **폴더 이름이 `SKILL.md`의 `name` 필드와 정확히 일치**해야 합니다.

- ✅ `skills/doc-automation/SKILL.md` (frontmatter `name: doc-automation`)
- ❌ `chapter2-doc-automation/` (name이 `doc-automation`이면 불일치 → 인식 안 됨)

그래서 학습용 챕터 폴더(`chapterN-*`)는 그대로 두고, **배포용 `skills/` 트리를 따로** 만들었습니다.

### ② 무엇을 넣고 무엇을 뺄까
- **넣기**: `SKILL.md`, `scripts/`, `templates/`, `references/`, 스킬이 실제로 쓰는 자산
- **빼기**: 책 원고(`book-chapter-*.md`), 생성 결과물(`output/`), 데모용 대용량 파일

### ③ `.skill`(zip) 형태 스킬은 압축 풀기
```bash
unzip excel-automation.skill -d skills/   # → skills/excel-automation/SKILL.md
```

### ④ frontmatter 규칙 점검
- `name`: 소문자·숫자·하이픈만, 양끝/연속 하이픈 금지, **폴더명과 동일**
- `description`: 1~1024자 (트리거 문구를 충분히 넣되 1024자 초과 금지)

---

## 2. 마켓플레이스 매니페스트 작성

저장소 루트에 **`.claude-plugin/marketplace.json`**:

```json
{
  "name": "claude-skills",
  "owner": {
    "name": "daje0601",
    "url": "https://github.com/stiron92-byte/claude-skills"
  },
  "metadata": {
    "description": "책에서 다루는 업무 자동화 스킬 모음",
    "version": "1.0.0"
  },
  "plugins": [
    {
      "name": "claude-skills-book",
      "description": "8개 업무 자동화 스킬 모음 ...",
      "source": "./",
      "strict": false,
      "skills": [
        "./skills/doc-automation",
        "./skills/excel-automation",
        "./skills/meeting-minutes",
        "./skills/content-research",
        "./skills/data-collector",
        "./skills/content-repurpose",
        "./skills/generate-shorts",
        "./skills/narration-video"
      ]
    }
  ]
}
```

- 최상위 `name` = **마켓플레이스 이름**, `plugins[].name` = **사용자가 설치하는 플러그인 이름** (둘이 달라도 됨)
- `source: "./"` = 플러그인 루트가 저장소 루트
- `skills[]` = 각 스킬 폴더 경로
- ⚠️ `metadata.repository`는 Claude Code가 **무시하고 경고**를 내므로 넣지 마세요 (저장소 주소는 `owner.url`로 충분)

### 공식 검증
```bash
claude plugin validate .
# → ✔ Validation passed
```
이 명령으로 매니페스트와 각 스킬 frontmatter를 **푸시 전에** 검증할 수 있습니다.

---

## 3. GitHub에 푸시 = 배포

마켓플레이스는 **공개 GitHub 저장소**여야 사용자가 받을 수 있습니다.
(특히 Cowork는 **공개 저장소만** 받습니다.)

### 플러그인 파일만 골라 커밋
```bash
git add .claude-plugin/ skills/ README.md
git commit -m "feat: add claude-skills-book marketplace (8 skills)"
git push origin main      # 반드시 '기본 브랜치(main)'에 있어야 함
```

### 푸시 전 확인
```bash
# 공개 여부 / 기본 브랜치
gh repo view <owner>/<repo> --json visibility,defaultBranchRef
# 내 푸시 권한 (push: true 여야 함)
gh api repos/<owner>/<repo> --jq '.permissions'
```

### ⚠️ 임베디드 git 저장소 주의
다른 사람의 저장소를 클론해 폴더로 갖고 있으면 그 안에 `.git`이 있어,
`git add .` 시 **gitlink(빈 포인터, mode 160000)** 로 들어가 망가집니다(클론하면 빈 폴더가 됨).
해당 폴더를 `.gitignore`에 넣고 스테이징에서 제외하세요.
```bash
echo "path/to/vendored-repo/" >> .gitignore
git restore --staged path/to/vendored-repo      # 이미 들어갔다면 제거
git diff --cached --raw | grep 160000            # 결과 없으면 OK
```

---

## 4. 설치하기

### 4-1. Claude Code (터미널 / 프롬프트)
프롬프트의 슬래시 명령 또는 터미널 CLI, 둘 다 동일하게 동작합니다.

```
/plugin marketplace add stiron92-byte/claude-skills
/plugin install claude-skills-book@claude-skills
```
```bash
claude plugin marketplace add stiron92-byte/claude-skills
claude plugin install claude-skills-book@claude-skills
claude plugin list      # 설치 확인
```

- 소스로 **GitHub repo / git URL / 로컬 경로** 모두 가능 → 로컬 경로는 푸시 전 테스트에 유용
- 설치 단위는 `<플러그인>@<마켓플레이스>` 형식 (`claude-skills-book@claude-skills`)
- `marketplace add`는 "등록", 실제 설치는 `install` — **두 단계**

### 4-2. Claude Cowork (데스크톱 앱)
> ⚠️ **"디렉터리" 검색창에 GitHub 주소를 쳐도 안 나옵니다.**
> 그 검색창은 **Anthropic 공식 카탈로그 전용**입니다. 커스텀 저장소는 검색이 아니라 아래 경로로 등록합니다.

1. **Customize** 메뉴 → **Plugins** 탭
2. **Personal plugins** 섹션에서 **"+"** 클릭
3. **"Add marketplace"** 선택
4. **"Add from a repository"** → `stiron92-byte/claude-skills` 입력
5. **"Browse plugins"** → `claude-skills-book` → **"Install"**

- Cowork는 **로컬 경로 불가**, **공개 GitHub 저장소/URL만** 지원 → 반드시 푸시 후 설치

설치하면 8개 스킬이 등록되고, "회의록 정리해줘"처럼 **자연어로 자동 트리거**됩니다.

---

## 5. 자주 막히는 지점 (실제로 겪은 것)

| 증상 | 원인 | 해결 |
|------|------|------|
| 디렉터리 검색에 안 뜸 | 검색창은 공식 카탈로그만 검색 | "Add marketplace"로 **등록** (검색 아님) |
| Cowork에서 로컬 경로 안 됨 | Cowork는 공개 git만 지원 | GitHub **푸시** 후 repo로 추가 |
| 클론한 폴더가 빈 채로 올라감 | 중첩 `.git`(임베디드 저장소) → gitlink | `.gitignore` + 언스테이징 |
| 스킬이 인식 안 됨 | 폴더명 ≠ `name` | 폴더명을 `name`과 일치 |
| `validate` 경고 | 미지원 필드(`metadata.repository` 등) | 해당 필드 제거 |
| 무거운 스킬 오류 | MCP 서버/API 키 필요(예: narration-video) | 자동활성화 대신 SETUP 안내로 분리 |

---

## 6. 업데이트 / 제거

```bash
# 마켓플레이스 갱신(저장소 최신화 반영)
claude plugin marketplace update claude-skills

# 제거
claude plugin uninstall claude-skills-book@claude-skills
claude plugin marketplace remove claude-skills
```
Cowork에서는 **Customize → Plugins**에서 갱신/비활성화/삭제합니다.

---

## 부록: 배포 체크리스트

- [ ] `skills/<name>/` 폴더명 = `SKILL.md`의 `name`
- [ ] 각 `SKILL.md`에 유효한 `name`(소문자·하이픈) + `description`(≤1024자)
- [ ] `.claude-plugin/marketplace.json` 작성 (`plugins[].skills` 경로 정확)
- [ ] `claude plugin validate .` 통과
- [ ] 임베디드 `.git` 없음 (`git diff --cached --raw | grep 160000` 비어있음)
- [ ] 시크릿/API 키 미포함
- [ ] **공개 저장소 + 기본 브랜치(main)** 에 push
- [ ] Claude Code / Cowork에서 설치 테스트

---

**참고 문서**
- [Use plugins in Claude Cowork](https://support.claude.com/en/articles/13837440-use-plugins-in-claude-cowork)
- [Browse skills, connectors, and plugins in one directory](https://support.claude.com/en/articles/14328846-browse-skills-connectors-and-plugins-in-one-directory)
- [Claude Code Plugins 공식 문서](https://code.claude.com/docs/en/plugins)
